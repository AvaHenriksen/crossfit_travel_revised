<head>
<style>
.cities_hero {
  background: url("http://crossfit110.com/wp-content/uploads/2017/01/OpenWorkout_Quintessential_0.jpg");
  background-size: cover;
  color: white;
  height: 500px;
  padding-top: 0px;
}
</style>

</head>

<body>
<div class = "cities_hero container-fluid">
  <div class="col-sm-8">
    <h1><strong>Welcome to CrossFit Travel Guide<strong></h1>
  </div>
  <div class= "col-sm-10 ">
    <h2>
         the one stop resource for athletes on the road
    </h2>
  </div>

</div>

<div class="page-header">
  <h1>
    Cities
    <% if current_user.admin? == true %>
      <a href="/cities/new" class="btn btn-lg btn-success">New City</a>
      <% else %>
    <% end %>
  </h1>
</div>

<div class="row mb-1">
  <div class="col-md-12">
    <%= search_form_for @q, :class => collapse_search_form, :id => "cities_filters" do |f| %>
      <p class="lead">Narrow results:</p>

        <div class="form-group">
    <%= f.label :name_cont, "Name contains" %>
    <%= f.text_field :name_cont, :class => "form-control", :placeholder => "Name contains" %>
  </div>

<div class="form-group">
    <%= f.label :region_cont, "Region contains" %>
    <%= f.text_field :region_cont, :class => "form-control", :placeholder => "Region contains" %>
  </div>

      <%= f.submit :class => "btn btn-primary btn-block mb-1" %>

      <a href="/cities" class="btn btn-default btn-block mb-1">Clear filters</a>
    <% end %>
    <a class="btn btn-default btn-block mb-1" data-toggle="collapse" href="#cities_filters">
      Show Filters
    </a>
  </div>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Name</th>
        <th>State</th>
        <th>Region</th>
        <th class="pull-right">Number of locations</th>
      </tr>

      <% @cities.each do |city| %>
      <tr>
        <td><a href="/cities/<%= city.id %>"><%= city.name %></a></td>
        <td><%= city.state %></td>
        <td><%= city.region %></td>
        <td class = "pull-right"><%= city.locations.count %></td>
      </tr>
      <% end %>
    </table>
    <%= paginate @cities, theme: 'twitter-bootstrap-4' %>
  </div>
</div>
</body>
